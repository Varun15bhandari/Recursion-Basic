class Solution {
    public static int sum(int[] arr, int i,int n){
        if(i>=n)return 0;
        return arr[i] + sum(arr,++i,n);
    }
    public int arraySum(int[] nums) {
        //your code goes here
        return sum(nums,0,nums.length);
    }
}
